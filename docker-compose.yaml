services:
  api:
    build: .
    container_name: fastapi_app
    ports:
      - "${APP_PORT}:${APP_PORT}"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    depends_on:
      - redis
    networks:
      - app-network

  redis:
    image: redis:6-alpine
    container_name: ${REDIS_HOST_URL}
    ports:
      - "${REDIS_HOST_PORT}:${REDIS_HOST_PORT}"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
